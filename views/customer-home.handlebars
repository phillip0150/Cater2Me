<div class="container customer-home-body-container">

    <h1 class="cust-name" id="name-of-customer">Welcome {{name}}</h1>

    <!-- CUSTOMER VIEW JOBS ROW -->
    <div class="row view-job-row">
        <div class="col-12-lg view-job-col">
            <table id="madeEventTable">
                <thead>
                    <tr>
                        <th>NAME</th>
                        <th>PHONE</th>
                        <th>CITY</th>
                        <th>STATE</th>
                        <th>OCCASION</th>
                        <th>SIZE</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each customer}}
                    <tr>
                        <td>{{name}}</td>
                        <td>{{phone}}</td>
                        <td>{{city}}</td>
                        <td>{{state}}</td>
                        <td>{{occasion}}</td>
                        <td>{{size}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>

    <!-- CUSTOMER ADD NEW EVENT BUTTON-ROW  -->
    <div class="row add-event-row">
        <div class="col-12-lg add-event-col">
            <a href="/event/create/{{userid}}"><button id="submit" class="btn btn-outline-light my-2 my-sm-0 custuser create-new-event">Create New Event</button></a>
            <a href="/logout"><button id="cookie" class="btn btn-outline-light my-2 my-sm-0 custuser" onclick="" >Log Out</button></a>
        </div>
    </div>
    <script type="text/javascript" src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <script src="/js/create.js"></script>